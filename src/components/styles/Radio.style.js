import styled from 'styled-components';
import { CheckboxContainer, CheckBoxLabel } from './Checkbox.style';

export const RadioContainer = styled(CheckboxContainer)``;

export const RadioLabel = styled(CheckBoxLabel)``;

export const RadioInput = styled.input``;

export const CustomRadioInput = styled.span`
  border-radius: 16px;
`;
